<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context=".MainActivity">

    <TextView
        android:id="@+id/app_title"
        style="@style/AppTheme.wrap"
        android:layout_above="@+id/start_talk"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="50dp"
        android:text="@string/app_name"
        android:textSize="40sp"/>

    <Button
        android:id="@+id/start_talk"
        style="@style/AppTheme.wrap"
        android:layout_above="@+id/setting_channel"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dp"
        android:background="@drawable/my_click"
        android:text="开始聊天"/>

    <Button
        android:id="@+id/setting_channel"
        style="@style/AppTheme.wrap"
        android:layout_centerInParent="true"
        android:layout_marginBottom="30dp"
        android:background="@drawable/my_click"
        android:text="设置频道"/>

    <Button
        android:id="@+id/change_name"
        style="@style/AppTheme.wrap"
        android:layout_below="@+id/setting_channel"
        android:layout_centerHorizontal="true"
        android:background="@drawable/my_click"
        android:text="更改名称"/>

    <Button
        android:id="@+id/app_introduce"
        style="@style/AppTheme.wrap"
        android:layout_below="@+id/change_name"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:background="@drawable/my_click"
        android:text="软件介绍"/>

</RelativeLayout>
